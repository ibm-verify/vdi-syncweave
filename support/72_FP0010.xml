<?xml version="1.0"?>

<!--
  ===========================================================================
     File:  72_FP0010.xml 

    Purpose:  Example shows how to create a full fixpack 8 of TDI 7.2.0.

    NOTE: All of these properties must be defined. Also
          only override the rules you need. The base ones are defined
          in install_zip_rules.xml.
-->

<project name="720_FP0010" basedir="." default="main">
	<target name="main">
	</target>

  <!-- Global variables for each IU to be built. -->

  <!--  BEGIN of Generic IU variables.  -->

  <!-- The type of IU packages we are creating.  In this case, its a fixpack.
       If the value is not Fixpack, its assumed to be an interim fix or la fix.
       Also, if its not a Fixpack, the version number is not used and will use
       the package name.
       MAIN_SUPPORT_PACKAGE_TYPE = FixPack | InterimFix | LimitedAvailabilityInterimFix
       NOTE: in SI 1.2.1 LimitedAvailabilityInterimFix is not yet recognized, so
       just use the FixPack for LAIFs also for now.-->
  <property name="SUPPORT_PACKAGE_TYPE"  value="FixPack"/>

  <!-- The version number for the IUs we are creating. For example, TDI 7.2.0 fixpack 1 would be 7.2.0.1 -->
  <property name="SUPPORT_PACKAGE_VERSION"  value="7.2.0.10"/>

  <!-- The name for the Main IU we are creating.
       This can be both the name of the Fixpack/IF/LAIF.
       If its a fixpack, you will only see this in the description
       of the package. -->
  <property name="SUPPORT_PACKAGE_NAME"  value="SDI-7.2-FP0010"/>

  <!-- More descriptive name for SWG Tagging process. -->
  <property name="SUPPORT_PACKAGE_DES_NAME"  value="IBM ${project.tdi.name} V${project.tdi.swgtag.vers} Fix Pack 10"/>

  <!-- The minimum version number the patch can be applied on top of. -->
  <property name="SUPPORT_MIN_VERSION"  value="7.2.0.0"/>

  <!-- The maximum version number the patch can be applied on top of. -->
  <property name="SUPPORT_MAX_VERSION"  value="7.2.0.9"/>

  <!--  END of Generic IU variables.  -->

  <!--  Tells whether the base should be included in the maintenance package.  -->
  <property name="BASE_PACKAGE_ENABLED"  value="true"/>

  <!--  Tells whether the server should be included in the maintenance package.  -->
  <property name="SERVER_PACKAGE_ENABLED"  value="true"/>

  <!--  Tells whether the ce should be included in the maintenance package.  -->
  <property name="CE_PACKAGE_ENABLED"  value="true"/>
  <!-- Note that when updating CE, you will need to update the project.tdi.version variable in project_setup.xml -->

  <!--  Tells whether the ce update site should be included in the maintenance package.  -->
  <property name="CE_UPDATE_PACKAGE_ENABLED"  value="true"/>

  <!--  Tells whether the examples should be included in the maintenance package.  -->
  <property name="EXAMPLES_PACKAGE_ENABLED"  value="true"/>

  <!--  Tells whether the javadocs should be included in the maintenance package.  -->
  <property name="JAVADOCS_PACKAGE_ENABLED"  value="true"/>

  <!--  Tells whether the amc should be included in the maintenance package.  -->
  <property name="AMC_PACKAGE_ENABLED"  value="false"/>

  <!--  Tells whether the license should be included in the maintenance package.  -->
  <!--  NOTE: Base must be true if license is true -->
  <property name="LICENSE_PACKAGE_ENABLED"  value="true"/>

  <!--  Tells whether a JRE should be included in the maintenance package.  -->
  <!--  NOTE: Base must be true if JRE is true -->
  <property name="JRE_PACKAGE_ENABLED"  value="false"/>

  <!--  Tells whether the plugins should be included in the maintenance package.  -->
  <property name="PLUGINS_PACKAGE_ENABLED"  value="true"/>

  <!--  Tells whether a zos packages should also be made for maintenance package.  -->
  <property name="ZOS_PACKAGE_ENABLED"  value="false"/>

  <!--  Tells whether LWI will  be updated in this maintenance package.  -->
  <property name="EWP_PACKAGE_ENABLED"  value="false"/>

  <!--  Now override the macros as needed to zip up what you need per IUs.
        Some macros may not need to be overwritten if you are replacing every file in an
        IU but its likely that you will not do this.  See install_zip_rules.xml for
        a list of macros. Here are the macros you can override at this time:
        Server - zipTDIServerPackage
        Base - zipBasePackage
        CE - zipTDICEPackage
        AMC - zipTDIAMCPackage
        Examples - zipTDIExamplesPackage
        JavaDocs - zipTDIJavaDocsPackage
        License - zipTDILicensePackage
        Plugins - zipTDIPwdPluginsPackage
        zos - tarTDIZOSPackage
  -->

  <description>
    =========================================================================
      Macro:  zipTagsFile

      Purpose:  Make the zip of the Tags file in a specified location.
    =========================================================================
  </description>
  <macrodef name="zipTagsFile">

     <!-- The zip file name and location for the tags package. -->
     <attribute name="output.name"/>

   <sequential>
        <!-- delete any existing zip files. -->
        <delete file="@{output.name}"/>
       
        <!-- Create the file. -->
        <create_fxtag_file outputname.loc="${project.root.export.swgtags}/IBM_Tivoli_Directory_Integrator.${SUPPORT_PACKAGE_VERSION}.fxtag"
                           swgfmidx="${prod_swgfmidx_name}" fixname="${SUPPORT_PACKAGE_DES_NAME}"
                           fixversion="${SUPPORT_PACKAGE_VERSION}" fixid="${SUPPORT_PACKAGE_NAME}" fixtype="${SUPPORT_PACKAGE_TYPE}"/>

        <!-- zip the file. -->
        <zip zipfile="@{output.name}">
            <zipfileset dir="${project.root.export.swgtags}"
                prefix="properties/version">
                <include name="IBM_Tivoli_Directory_Integrator.${SUPPORT_PACKAGE_VERSION}.fxtag"/>
            </zipfileset>
        </zip>

   </sequential>
  </macrodef>

  <description>
    =========================================================================
      Macro:  zipTDIServerPackage

      Purpose:  Make the zip of the TDI Server package in a specified location.
    =========================================================================
  </description>
  <macrodef name="zipTDIServerPackage">
   <!-- The zip file name and location for the base server package. -->
   <attribute name="output.name.base"/>
   <!-- The zip file name and location for the identity components only to be shipped with identity Edition. -->
   <attribute name="output.name.identity"/>
   <!-- The zip file name and location for the windows server package. -->
   <attribute name="output.name.win"/>
   <!-- The zip file name and location for the unix server package. -->
   <attribute name="output.name.unix"/>
   <!-- The zip file name and location for the i5os server package. -->
   <attribute name="output.name.i5os"/>
   <!-- The zip file name and location for the Windows x86 server package. -->
   <attribute name="output.name.win.x86"/>
   <!-- The zip file name and location for the Linux x86 server package. -->
   <attribute name="output.name.linux.x86"/>
   <!-- The zip file name and location for the AIX server package. -->
   <attribute name="output.name.aix"/>
   <!-- The zip file name and location for the Windows 64 bit server package. -->
   <attribute name="output.name.win.x86.64"/>

   <sequential>
        <!-- delete any existing zip files. -->
        <delete file="@{output.name.base}"/>
        <delete file="@{output.name.identity}"/>
        <delete file="@{output.name.win}"/>
        <delete file="@{output.name.unix}"/>
        <delete file="@{output.name.i5os}"/>
        <delete file="@{output.name.win.x86}"/>
        <delete file="@{output.name.linux.x86}"/>
        <delete file="@{output.name.aix}"/>
        <delete file="@{output.name.win.x86.64}"/>

        <!-- The following three zip files should only be updated in the 
             event there is a LUM jni fix -->

        <zip zipfile="@{output.name.win.x86}">
            <zipfileset dir="${project.root.lib}"
                prefix="libs">
                <include name="domchdet.dll"/>
                <include name="COMProxy.dll"/>
            </zipfileset>
	    <zipfileset dir="${project.root}/src/com/ibm/di/Win32/WinService/bin"
                prefix="win32_service">
                <include name="ibmdiservice.exe"/>
            </zipfileset>
        </zip>

	
        <zip zipfile="@{output.name.win.x86.64}">
		<zipfileset dir="${project.root.lib}/64"
                prefix="libs">
                <include name="domchdet.dll"/>
                <include name="COMProxy.dll"/>
            </zipfileset>
	    <zipfileset dir="${project.root}/src/com/ibm/di/Win32/WinService/bin64"
                prefix="win32_service">
                <include name="ibmdiservice.exe"/>
            </zipfileset>
        </zip>

<!--
        <zip zipfile="@{output.name.linux.x86}">
            <zipfileset dir="${root.lum_src}"
                prefix="libs">
                <include name="libi4trybuyjni.so"/>
            </zipfileset>
        </zip>
-->		

<!--
        <zip zipfile="@{output.name.aix}">
            <zipfileset dir="${root.lum_src}"
                prefix="libs">
                <include name="libi4trybuyjni.a"/>
            </zipfileset>
        </zip>
-->

	<!-- Zip of the windows zip.  -->
        <zip zipfile="@{output.name.win}">
            <zipfileset dir="${project.root.export.win.bin}" >
                <include name="ibmdisrv.bat"/>
            </zipfileset>
        </zip>

        <!-- Zip of the unix zip.  -->
        <zip zipfile="@{output.name.unix}">
	    <zipfileset dir="${project.root.export.unix.bin}" >
                <include name="ibmdisrv"/>
            </zipfileset>
        </zip>

        <!-- Zip of the i5OS zip.  -->
        <zip zipfile="@{output.name.i5os}">
	    <zipfileset dir="${project.root.export.i5os.bin}" >
                <include name="ibmdisrv"/>
            </zipfileset>
        </zip>

        <!-- One for the identity specific components that only ship with the identity edition. 
        <zip zipfile="@{output.name.identity}">
           <zipfileset dir="${project.root.export}" >
              <include name="jars/connectors/WindowsUsersGroupsConnector.jar"/>
           </zipfileset>
        </zip>
		 -->

        <!-- One for generic not platform specific items (where possible).  -->
        <!-- NOTE: build.properties should always be included when the server is updated-->
        <zip zipfile="@{output.name.base}">
		
	    <zipfileset dir="${project.root.export}" >
                <include name="IDILoader.jar" />
            </zipfileset>

			<!-- Jars in the ibmjs engine folder -->
            <zipfileset dir="${root.jsengine_src}"
                prefix="jars/3rdparty/IBM">
                <include name="ibmjs.jar"/>
            </zipfileset>

        	<!-- Add TDSJNDIToolkit.jar -->
            <zipfileset dir="${root.tds_jnditoolkit_src}"
                prefix="jars/3rdparty/IBM">
                    <include name="TDSJNDIToolkit.jar"/>
            </zipfileset>

        	<!-- Update MQe version -->
            <zipfileset dir="${root.mqe_src}"
                prefix="jars/3rdparty/IBM">
                    <include name="ibmjms.jar"/>
                    <include name="MQeBase.jar"/>
                    <include name="MQeJMS.jar"/>
                    <include name="MQeSecurity.jar"/>
            </zipfileset>
        	
        	<!-- snmp.jar -->
            <zipfileset dir="${root.tivsnmp_src}"
                prefix="jars/3rdparty/IBM">
                    <include name="snmp.jar"/>
            </zipfileset>

        	<!-- dsml.jar -->
            <zipfileset dir="${root.tds_dsml_src}"
                prefix="jars/3rdparty/IBM">
                    <include name="dsml.jar"/>
            </zipfileset>

            <zipfileset dir="${project.root.export}"
                prefix="etc/">
                <include name="build.properties"/>
            </zipfileset>

            <zipfileset dir="${project.root.etc}"
                prefix="etc/">
                <include name="log4j2.xml"/>
                <include name="ce-log4j2.xml"/>
                <include name="tdimiggbl-log4j.properties"/>
                <include name="tdisrvctl-log4j.properties"/>
                <include name="updateinstaller-log4j.properties"/>
            </zipfileset>

        	<!-- Jars in the jars folder -->
			<zipfileset dir="${project.root.export}/jars"
				prefix="jars">
					<include name="ldapsync.jar"/>
			</zipfileset>
			
        	<!-- AXIS2 Jar files -->
        	<zipfileset dir="${root.axis2_src}"
     			prefix="jars/3rdparty/IBM/axis2">
 				<include name="*.jar"/>	
				<exclude name="woodstox-core-asl-4.2.0.jar" />
				<exclude name="xmlbeans-3.0.0.jar" />
		</zipfileset>
 
        	<!-- Jars in the common folder -->
			<zipfileset dir="${project.root.export}/jars"
				prefix="jars/common">
					<include name="miserver.jar"/>
					<include name="mmconfig.jar"/>
					<include name="miconfig.jar"/>
					<include name="diserverapi.jar"/>
					<include name="diserverapirmi.jar"/>
					<include name="cli.jar"/>
					<include name="WebServiceUtil.jar"/>
			</zipfileset>
			
        	<!-- Jars for connectors -->
			<zipfileset dir="${project.root.export}/jars/connectors"
				prefix="jars/connectors">
					<include name="*.jar"/>
				</zipfileset>
			
        	<!-- Jars for parsers -->
			<zipfileset dir="${project.root.export}/jars/parsers"
				prefix="jars/parsers">
					<include name="*.jar"/>
				</zipfileset>
			
        	<!-- Jars for function componentes -->
			
			<zipfileset dir="${project.root.export}/jars/functions"
				prefix="jars/functions">
                                <include name="Axis2WSClientFC.jar"/>
                        	<include name="ComplexTypesGeneratorFC.jar"/>
                        	<include name="DeltaFC.jar"/>
                        	<include name="InvokeSoapWSFC.jar"/>
                        	<include name="JavaClassFC.jar"/>
                        	<include name="SapR3RfcFC.jar"/>
                        	<include name="SapR3RfcFCV3.jar"/>
                        	<include name="SendEMailFC.jar"/>
			</zipfileset>	
			
			<!-- Jars for OSGI Plugins.  -->			
			<zipfileset dir="${project.root.export.osgi}/embedded"
				prefix="osgi/plugins">
					<include name="org.eclipse.equinox.http.jetty.listener.jar"/>
					<include name="com.ibm.di.ui.webui.jar"/>
					<include name="com.ibm.di.api.rest.jar"/>	
					<include name="com.ibm.di.ui.easyetl.jar"/>
					<include name="com.ibm.di.config.bind.jar"/>
					<include name="com.ibm.di.api.bind.jar"/>
					<include name="com.ibm.tdi.rest.jar"/>
            </zipfileset>
        
			<!-- Zip up the LDAPSync folder -->
			<zipfileset dir="${project.root}/LDAPSync/LDAPSync" 
			prefix="LDAPSync">
				<include name="LDAPSync.xml"/>
				<include name="FDS_ISAM_Plugin.xml"/>
				<include name="FDS_ISAM_Plugin.map"/>
				<include name="FDS_Target_SCIM.xml"/>
				<include name="QRadar.map"/>
				<include name="SNMP.map"/>
				<include name="container.map"/>
				<include name="customScript.js"/>
				<include name="IBM-FDS-MIB.txt"/>
			</zipfileset>
				
			<!-- Zip up the SCIM folder -->
			<zipfileset dir="${project.root}/SCIM" 
			prefix="SCIM">
				<include name="ReadMe.txt"/>
				<include name="SCIM.xml"/>
				<include name="SCIM.properties.reference"/>
			</zipfileset>
        	
	        	<!-- RXA Jar Files -->
	        	<zipfileset dir="${root.rxa_src}"
        		prefix="jars/3rdparty/others">
        			<include name="jlanclient.jar"/>
        		</zipfileset>

			<!-- log4j v2 files -->
        		<zipfileset dir="${root.log4j_src}"
        			prefix="jars/3rdparty/others">
		    		<include name="log4j-1.2-api-2.17.1.jar"/>
		    		<include name="log4j-api-2.17.1.jar"/>
		    		<include name="log4j-core-2.17.1.jar"/>
        		</zipfileset>

			<zipfileset dir="${root.rxa_src}"
				prefix="jars/3rdparty/IBM">
				<include name="remoteaccess.jar"/>
				<include name="ssh.jar"/>
				<include name="rxa-langpack.jar"/>
			</zipfileset>
			
			<!-- ActiveMQ files -->
			<zipfileset dir="${root.activemq_src}"
				prefix="jars/3rdparty/others/ActiveMQ">
				<include name="activemq-all.jar"/>
				<include name="geronimo-j2ee-management_1.1_spec-1.0.1.jar"/>
				<include name="geronimo-jms_1.1_spec-1.1.1.jar"/>
				<include name="geronimo-jta_1.1_spec-1.1.1.jar"/>
				<include name="jackson-annotations.jar"/>
				<include name="jackson-core.jar"/>
				<include name="jackson-databind.jar"/>
				<include name="kahadb.jar"/>
				<include name="xbean-spring.jar"/>				
			</zipfileset>			
		</zip>

   </sequential>
  </macrodef>

  <description>
    =========================================================================
      Macro:  zipTDIBasePackage

      Purpose:  Make the zip of the TDI Base package in a specified location.
    =========================================================================
  </description>
  <macrodef name="zipTDIBasePackage">
   <!-- The zip file name and location for the general base package. -->
   <attribute name="output.name.base"/>
   <!-- The zip file name and location for the windows base package. -->
   <attribute name="output.name.win"/>
   <!-- The zip file name and location for the unix base package. -->
   <attribute name="output.name.unix"/>
   <!-- The zip file name and location for the i5os base package. -->
   <attribute name="output.name.i5os"/>

   <sequential>
        <!-- delete any existing zip files. -->
        <delete file="@{output.name.base}"/>
        <delete file="@{output.name.win}"/>
        <delete file="@{output.name.unix}"/>
        <delete file="@{output.name.i5os}"/>

   	<!--
	<zip zipfile="@{output.name.base}">

	<zipfileset dir="${project.root.export}/jars"
	 	prefix="maintenance">
	 	<include name="UpdateInstaller.jar"/>
 	</zipfileset>
 	</zip>
    -->
   	
 	 <!-- Package of the general files. 
        <zip zipfile="@{output.name.base}">
            <zipfileset dir="${root.icu4j_src}"
                prefix="jars/3rdparty/IBM">
                <include name="icu4j_4_0.jar"/>
            </zipfileset>
        </zip>
 -->
 
 	<!-- Package of the windows files.-->
        <zip zipfile="@{output.name.win}">
            <zipfileset dir="${project.root.export.win.bin}"
                prefix="bin/">
	          <include name="applyUpdates.new.bat"/>
	          <include name="tdiBackup.bat"/>
	          <include name="tdimiggbl.bat"/>
	          <include name="tdisrvctl.bat"/>
                  <include name="setupCmdLine.bat"/>
                  <include name="updateLog4j.bat"/>
                  <include name="removeAMC.bat"/>
		  <include name="moveActiveMQ.bat"/>
            </zipfileset>
	    <zipfileset dir="${project.root.serverapi}"
		prefix="serverapi/">
		<include name="cryptoutils.bat" />
            </zipfileset>
	    <zipfileset dir="${project.root.serverapi}"
		prefix="win32_service/serverapi/">
		<include name="cryptoutils.bat" />
            </zipfileset>
        </zip>
		
        <!-- Package of the unix files.  -->
        <zip zipfile="@{output.name.unix}">
            <zipfileset dir="${project.root.export.unix.bin}"
                prefix="bin/">
	          <include name="applyUpdates.new.sh"/>
	          <include name="tdiBackup.sh"/>
	          <include name="tdimiggbl"/>
	          <include name="tdisrvctl"/>
                  <include name="setupCmdLine.sh"/>
                  <include name="updateLog4j.sh"/>
                  <include name="removeAMC.sh"/>
		  <include name="moveActiveMQ.sh"/>
            </zipfileset>
	    <zipfileset dir="${project.root.serverapi}"
		prefix="serverapi/">
		<include name="cryptoutils.sh" />
            </zipfileset>
	    <zipfileset dir="${project.root.performance}"
		prefix="performance/">
		<include name="ibmdisrvtp.sh" />
            </zipfileset>
        </zip>
       

        <!-- Package of the i5OS files. 
        <zip zipfile="@{output.name.i5os}">
            <zipfileset dir="${project.root.export.i5os.bin}"
                prefix="bin/">
                <include name="setupCmdLine.sh"/>
                <include name="tdisrvctl"/>
            </zipfileset>
        </zip>
         -->

   </sequential>
  </macrodef>

  <description>
    =========================================================================
      Macro:  zipTDICEPackage

      Purpose:  Make the zip of the TDI CE package in a specified location.
    =========================================================================
  </description>
  <macrodef name="zipTDICEPackage">
    <!-- Note that when updating CE, you will need to update the project.tdi.version
         variable in project_setup.xml -->

   <!-- The zip file name and location for the base CE package. -->
   <attribute name="output.name.base"/>
   <!-- The zip file name and location for the windows CE package. -->
   <attribute name="output.name.win"/>
   <!-- The zip file name and location for the unix CE package. -->
   <attribute name="output.name.unix"/>
   <!-- The zip file name and location for the base CE RCP package. -->
   <attribute name="output.name.basercp"/>
   <!-- The zip file name and location for the AIX CE RCP package. -->
   <attribute name="output.name.aixrcp"/>
   <!-- The zip file name and location for the Linux CE RCP package. -->
   <attribute name="output.name.linuxrcp"/>
   <!-- The zip file name and location for the Linux 64 CE RCP package. -->
   <attribute name="output.name.linux64rcp"/>
   <!-- The zip file name and location for the Solaris CE RCP package. -->
   <attribute name="output.name.solarisrcp"/>
   <!-- The zip file name and location for the Win 32 CE RCP package. -->
   <attribute name="output.name.win32rcp" />
   <!-- The zip file name and location for the Win 64 CE RCP package. -->
   <attribute name="output.name.win64rcp" />

   <sequential>
        <!-- delete any existing zip files. -->
        <delete file="@{output.name.base}"/>
        <delete file="@{output.name.win}"/>
        <delete file="@{output.name.unix}"/>  
        <delete file="@{output.name.basercp}"/>
        <delete file="@{output.name.aixrcp}"/>
        <delete file="@{output.name.linuxrcp}"/>
        <delete file="@{output.name.linux64rcp}"/>
        <delete file="@{output.name.solarisrcp}"/>
        <delete file="@{output.name.win32rcp}"/>
        <delete file="@{output.name.win64rcp}"/>

        <!-- Base CE files -->
        <zip zipfile="@{output.name.base}">
            <zipfileset dir="${project.root.export}/ce_eclipse">
                <include name="eclipsece/plugins/${project.root.ce_rcp.loader.plugin.name}_${project.tdi.version}.jar" />
                <include name="eclipsece/plugins/${project.root.ce_rcp.base.plugin.name}_${project.tdi.version}.jar" />                    
            </zipfileset>            
        </zip>

        <!-- One for Windows. -->
<!--
        <zip zipfile="@{output.name.win}">
            <zipfileset dir="${project.root.export.win.bin}" >
                <include name="ibmditk.bat"/>
            </zipfileset>
        </zip>
-->
        <!-- One for UNIX. -->
<!--
        <zip zipfile="@{output.name.unix}">
            <zipfileset dir="${project.root.export.unix.bin}" >
                <include name="ibmditk"/>
            </zipfileset>
        </zip>
-->
        
        <!-- Base CE RCP files -->
<!--
        <zip zipfile="@{output.name.basercp}">        
            <zipfileset dir="${project.tools_home}/x86_nt_4/eclipse/eclipse_runtime/${root.eclipse_runtime_vers}/eclipse" 
                prefix="eclipsece">
                <include name="plugins/com.jcraft.jsch_0.1.31.jar"/>
            </zipfileset>
        </zip>
-->
        <!-- Use the following 4 definitions for platform specific RCP jars -->
        <!-- AIX RCP -->
<!--
        <zip zipfile="@{output.name.aixrcp}">
             
            <zipfileset dir="${project.tools_home}/x86_nt_4/eclipse/eclipse_runtime/${root.eclipse_runtime_vers}/eclipse" 
                prefix="eclipsece">
                <include name="plugins/org.eclipse.swt.motif.aix.ppc_3.3.1.v3349.jar"/>
            </zipfileset>
        </zip>
-->
        <!-- Linux RCP -->
<!--
        <zip zipfile="@{output.name.linuxrcp}">
            <zipfileset dir="${project.tools_home}/x86_nt_4/eclipse/eclipse_runtime/${root.eclipse_runtime_vers}/eclipse" 
                prefix="eclipsece">
                <include name="plugins/org.eclipse.swt.gtk.linux.x86.nl1_3.3.2.v200802282150.jar"/>
            </zipfileset>
        </zip>
-->
        <!-- Linux 64 RCP -->
<!--
        <zip zipfile="@{output.name.linux64rcp}">
            <zipfileset dir="${project.tools_home}/x86_nt_4/eclipse/eclipse_runtime/${root.eclipse_runtime_vers}/eclipse" 
                prefix="eclipsece">
                <include name="plugins/org.eclipse.core.filesystem.linux.x86_64_1.0.100.v20080604-1400.jar"/>
            </zipfileset>
        </zip>
-->
        <!-- Solaris RCP -->
<!--
        <zip zipfile="@{output.name.solarisrcp}">
            <zipfileset dir="${project.tools_home}/x86_nt_4/eclipse/eclipse_runtime/${root.eclipse_runtime_vers}/eclipse"
                prefix="eclipsece">

                <include name="plugins/org.eclipse.core.filesystem.solaris.sparc_1.0.100.v20070425.jar"/>
            </zipfileset>
        </zip>
-->
        <!-- Win 32 RCP -->
<!--
        <zip zipfile="@{output.name.win32rcp}">
            <zipfileset dir="${project.tools_home}/x86_nt_4/eclipse/eclipse_runtime/${root.eclipse_runtime_vers}/eclipse"
                prefix="eclipsece">
                <include name="plugins/org.eclipse.swt.win32.win32.x86.nl1_3.3.3.v200802282150.jar"/>
            </zipfileset>
        </zip>
-->        
        <!-- Win 64 RCP -->
<!--
        <zip zipfile="@{output.name.win64rcp}">
            <zipfileset dir="${project.tools_home}/x86_nt_4/eclipse/eclipse_runtime/${root.eclipse_runtime_vers}/eclipse"
                prefix="eclipsece">
                <include name="plugins/org.eclipse.swt.win32.win32.x86_64_3.4.1.v3449c.jar"/>
            </zipfileset>
        </zip>
-->       

   </sequential>
  </macrodef>

  <description>
    =========================================================================
      Macro:  zipTDIAMCPackage

      Purpose:  Make the zip of the TDI AMC package in a specified location.
    =========================================================================
  </description>
  <macrodef name="zipTDIAMCPackage">
   <!-- The zip file name and location for the base AMC package. -->
   <attribute name="output.name.base"/>
   <!-- The zip file name and location for the windows AMC package. -->
   <attribute name="output.name.win"/>
   <!-- The zip file name and location for the unix AMC package. -->
   <attribute name="output.name.unix"/>
   <!-- The zip file name and location for the i5os AMC package. -->
   <attribute name="output.name.i5os"/>

   <sequential>
        <!-- delete any existing zip files. -->
        <delete file="@{output.name.base}"/>
        <delete file="@{output.name.win}"/>
        <delete file="@{output.name.unix}"/>
        <delete file="@{output.name.i5os}"/>

        <!-- One for Windows. -->
        <zip zipfile="@{output.name.win}">
            <zipfileset dir="${project.root.export.win.bin}" prefix="bin/amc/">
                <include name="backupamc.bat" />
                <include name="backupamcdb.bat" />
                <include name="restoreamcdb.bat" />
                <include name="startAM.bat" />
                <include name="tdimigam.bat" />
                <include name="tdimigamc.bat" />
            </zipfileset>
        </zip>

        <!-- One for UNIX. -->
        <zip zipfile="@{output.name.unix}">
            <zipfileset dir="${project.root.export.unix.bin}" prefix="bin/amc/">
                <include name="backupamc.sh" />
                <include name="backupamcdb.sh" />
                <include name="restoreamcdb.sh" />
                <include name="startAM.sh" />
                <include name="tdimigam.sh" />
                <include name="tdimigamc.sh" />
            </zipfileset>
        </zip>

        <!-- One for i5os. -->
        <zip zipfile="@{output.name.i5os}">
            <zipfileset dir="${project.root.export.i5os.bin}" prefix="bin/amc/">
                <include name="backupamc.sh" />
                <include name="backupamcdb.sh" />
                <include name="restoreamcdb.sh" />
                <include name="startAM.sh" />
                <include name="tdimigam.sh" />
                <include name="tdimigamc.sh" />
            </zipfileset>
        </zip>

        <!-- One for common zip. -->
        <zip zipfile="@{output.name.base}">
            <zipfileset dir="${project.root.export.amc.ship}/AMC" prefix="amc/">
                <include name="tdiamc.war"/>
            </zipfileset>
            <zipfileset dir="${project.root.export.amc.ship}/AMC/iscse" prefix="amc/iscse">
	         <include name="**/*"/>
            </zipfileset>
		<zipfileset dir="${project.root.am.src}"
			prefix="bin/amc/ActionManager">
			<include name="tdimigam-log4j.properties"/>
		</zipfileset>
		<zipfileset dir="${project.root.amc.src}"
			prefix="bin/amc/ActionManager">
			<include name="tdimigamc-log4j.properties"/>
		</zipfileset>
		<zipfileset dir="${project.root.amc_migration.src}"
			prefix="bin/amc/ActionManager">
			<include name="tdimigamcdb-log4j.properties"/>
		</zipfileset>
		<zipfileset dir="${root.rxa_src}"
			prefix="bin/amc/ActionManager/jars">
			<include name="remoteaccess.jar"/>
			<include name="ssh.jar"/>
			<include name="rxa-langpack.jar"/>
		</zipfileset>
        	<zipfileset dir="${root.log4j_src}"
			prefix="bin/amc/ActionManager/jars">
	    		<include name="log4j-1.2-api-2.17.1.jar"/>
	    		<include name="log4j-api-2.17.1.jar"/>
	    		<include name="log4j-core-2.17.1.jar"/>
        	</zipfileset>
        </zip>
   </sequential>
  </macrodef>

  <description>
    =========================================================================
      Macro:  zipTDILicensePackage

      Purpose:  Make the zip of the TDI license package in a specified location.
    =========================================================================
  </description>
  <macrodef name="zipTDILicensePackage">
   <!-- The zip file name and location for the license package. -->
   <attribute name="output.name"/>

   <sequential>
      <delete dir="${project.root.license.export}"/>
      <mkdir dir="${project.root.license.export}"/>
       <!-- Copy over the license files needed. -->
       <copy todir="${project.root.license.export}">
         <fileset dir="${project.root.license}">
           <include name="*"/>
         </fileset>
       </copy>
       <!-- Create the proper license files. -->
        <concat destfile="${project.root.license.export}/TDI_License_zh.txt" binary="true" append="true">
            <filelist dir="${project.root.license.export}" files="LA_zh,LI_zh"/>
        </concat>
        <concat destfile="${project.root.license.export}/TDI_License_zh_TW.txt" binary="true" append="true">
            <filelist dir="${project.root.license.export}" files="LA_zh_TW,LI_zh_TW"/>
        </concat>
        <concat destfile="${project.root.license.export}/TDI_License_cs.txt" binary="true" append="true">
            <filelist dir="${project.root.license.export}" files="LA_cs,LI_cs"/>
        </concat>
        <concat destfile="${project.root.license.export}/TDI_License_en.txt" binary="true" append="true">
            <filelist dir="${project.root.license.export}" files="LA_en,LI_en"/>
        </concat>
        <concat destfile="${project.root.license.export}/TDI_License_fr.txt" binary="true" append="true">
            <filelist dir="${project.root.license.export}" files="LA_fr,LI_fr"/>
        </concat>
        <concat destfile="${project.root.license.export}/TDI_License_de.txt" binary="true" append="true">
            <filelist dir="${project.root.license.export}" files="LA_de,LI_de"/>
        </concat>
        <concat destfile="${project.root.license.export}/TDI_License_it.txt" binary="true" append="true">
            <filelist dir="${project.root.license.export}" files="LA_it,LI_it"/>
        </concat>
        <concat destfile="${project.root.license.export}/TDI_License_ja.txt" binary="true" append="true">
            <filelist dir="${project.root.license.export}" files="LA_ja,LI_ja"/>
        </concat>
        <concat destfile="${project.root.license.export}/TDI_License_ko.txt" binary="true" append="true">
            <filelist dir="${project.root.license.export}" files="LA_ko,LI_ko"/>
        </concat>
        <concat destfile="${project.root.license.export}/TDI_License_pl.txt" binary="true" append="true">
            <filelist dir="${project.root.license.export}" files="LA_pl,LI_pl"/>
        </concat>
        <concat destfile="${project.root.license.export}/TDI_License_pt_BR.txt" binary="true" append="true">
            <filelist dir="${project.root.license.export}" files="LA_pt,LI_pt"/>
        </concat>
        <concat destfile="${project.root.license.export}/TDI_License_es.txt" binary="true" append="true">
            <filelist dir="${project.root.license.export}" files="LA_es,LI_es"/>
        </concat>
        <concat destfile="${project.root.license.export}/TDI_License_tr.txt" binary="true" append="true">
            <filelist dir="${project.root.license.export}" files="LA_tr,LI_tr"/>
        </concat>
        <copy file="${project.root.license.export}/TDI_License_en.txt"
              tofile="${project.root.license.export}/TDI_License.txt"/>

        <!-- Delete the old zip file if it exists. -->
        <delete file="@{output.name}"/>

        <!-- Make the license zip file. -->
        <zip zipfile="@{output.name}">
            <zipfileset dir="${project.root.license.export}/"
                prefix="license">
                <include name="TDI_License.txt"/>
                <include name="TDI_License_zh.txt"/>
                <include name="TDI_License_zh_TW.txt"/>
                <include name="TDI_License_cs.txt"/>
                <include name="TDI_License_en.txt"/>
                <include name="TDI_License_fr.txt"/>
                <include name="TDI_License_de.txt"/>
                <include name="TDI_License_it.txt"/>
                <include name="TDI_License_ja.txt"/>
                <include name="TDI_License_ko.txt"/>
                <include name="TDI_License_pl.txt"/>
                <include name="TDI_License_pt_BR.txt"/>
                <include name="TDI_License_es.txt"/>
                <include name="TDI_License_tr.txt"/>
            </zipfileset>
            <zipfileset dir="${project.root.license.export}"
                prefix="license">
                <include name="notices"/>
                <include name="non_ibm_license"/>
            </zipfileset>
        </zip>
   </sequential>
  </macrodef>

  <description>
    =========================================================================
      Macro:  zipTDIJavaDocsPackage

      Purpose:  Make the zip of the TDI Javadocs package in a specified location.
    =========================================================================
  </description>
  <macrodef name="zipTDIJavaDocsPackage">
   <!-- The zip file name and location for the license package. -->
   <attribute name="output.name"/>

   <sequential>
      <!-- Delete the old zip file if it exists. -->
      <delete file="@{output.name}"/>

      <!-- Make the Javadocs zip file. -->
      <zip zipfile="@{output.name}">
         <zipfileset dir="${project.root.export}" >
            <include name="docs/**/*"/>
         </zipfileset>
      </zip>
   </sequential>
  </macrodef>

  <description>
    =========================================================================
      Macro:  zipTDIExamplesPackage

      Purpose:  Make the zip of the TDI Examples package in a specified location.
    =========================================================================
  </description>
  <macrodef name="zipTDIExamplesPackage">
   <!-- The zip file name and location for the example package. -->
   <attribute name="output.name"/>
   <!-- The zip file name and location for examples that are added to the identity edition. -->
   <attribute name="output.name.identity.ed"/>
   <!-- Determines whether the format of the txt files should be dos or unix.
        Specify either dos or unix. -->
   <attribute name="eol"/>

   <sequential>
      <!-- Delete the old zip file if it exists. -->
      <delete file="@{output.name}"/>

      <!-- Make sure the needed directory is created. -->
      <delete dir="${project.root.examples.export}"/>
      <mkdir dir="${project.root.examples.export}"/>
      <!-- Copy over the files needed -->
      <copy todir="${project.root.examples.export}">
        <fileset dir="${project.root.examples}">
          <include name="**/*"/>
        </fileset>
      </copy>

      <!-- Put the files in the format specified. -->
      <fixcrlf srcdir="${project.root.examples.export}"
               eol="@{eol}"
               includes="**/*.txt" />
			   
	<!-- Include Virt dir example -->
	
	
	<zip zipfile="@{output.name}">	          
	 	     <zipfileset dir="${project.root.export}" >
				 <include name="examples/VirtDir/*"/>
				 <include name="examples/sca/*"/>
			 </zipfileset>   	 	         
     </zip>	
	

     <!-- Make the Examples zip file. 
      <zip zipfile="@{output.name}">
          <zipfileset dir="${project.root.export}" >
              <include name="examples/jms/*"/>
          </zipfileset>
      </zip>
 
      <zip zipfile="@{output.name.identity.ed}">
          <zipfileset dir="${project.root.export}" >
              <include name="examples/SPMLv2Parser/*"/>
          </zipfileset>
      </zip>
	-->
   </sequential>
  </macrodef>

  <description>
    =========================================================================
      Macro:  zipCommonPluginJars

      Purpose:  Adds common plugin jars to a zip file.
    =========================================================================
  </description>
  <macrodef name="zipCommonPluginJars">
    <!-- Name and location of the zip file being produced. -->
    <attribute name="plugin.zip"/>
    <!-- The prefix to put in the zip where all the jar files will go. -->
    <attribute name="plugin.prefix.dir"/>

    <sequential>
      <!-- Create the base jars package that is used by every plugin. -->
      <zip zipfile="@{plugin.zip}"  update="true">
          <zipfileset dir="${project.root.export}/jars/plugins"
                      prefix="@{plugin.prefix.dir}/jars">
              <include name="proxy.jar" />
              <include name="mqeconfig.jar" />
          </zipfileset>
	  <zipfileset dir="${project.root.pwd.plugin.src}/etc"
                      prefix="@{plugin.prefix.dir}/etc">
		      <include name="migpwsync-log4j.properties" />
	</zipfileset>
	<!-- ActiveMQ file -->
	<zipfileset dir="${root.activemq_src}"
		prefix="@{plugin.prefix.dir}/jars">
		<include name="activemq-all.jar"/>
		<include name="geronimo-j2ee-management_1.1_spec-1.0.1.jar"/>
		<include name="geronimo-jms_1.1_spec-1.1.1.jar"/>
		<include name="geronimo-jta_1.1_spec-1.1.1.jar"/>
	</zipfileset>	      
      </zip>
    </sequential>

  </macrodef>

  <description>
    =========================================================================
      Macro:  zipCommonPluginFiles

      Purpose:  Adds generic specific common plugin files to a zip file.
    =========================================================================
  </description>
  <macrodef name="zipCommonPluginFiles">
    <!-- Name and location of the zip file being produced. -->
    <attribute name="plugin.zip"/>
    <!-- The prefix to put in the zip where all the jar files will go. -->
    <attribute name="plugin.prefix.dir"/>
    <!-- The format to put the files in. (dos or unix). -->
    <attribute name="plugin.file.format"/>
    <!-- The script suffixes to bring in. -->
    <attribute name="plugin.file.suffix"/>

    <sequential>
        <!-- Put the files in the format we need. 
        <fixcrlf srcdir="${project.root.pwd.plugin.export}/bin"
                 eol="@{plugin.file.format}"
                 includes="*.props *.@{plugin.file.suffix}" />
        <fixcrlf srcdir="${project.root.pwd.plugin.export}/etc"
                 eol="@{plugin.file.format}"
                 includes="*.ldif" />-->
        <!-- Package up the scripts, a few props files, and ldif files.
        <zip zipfile="@{plugin.zip}"  update="true">
            <zipfileset dir="${project.root.pwd.plugin.export}/bin"
                        prefix="@{plugin.prefix.dir}/IDS">
               <include name="mqeconfig.props"/>
            </zipfileset>
        </zip> -->
        <zip zipfile="@{plugin.zip}"  update="true">
	  <zipfileset dir="${project.root.pwd.plugin.src}/plns_bin"
                      prefix="@{plugin.prefix.dir}/bin">
	      <include name="migpwsync.@{plugin.file.suffix}"/>
          </zipfileset>
	</zip>
    </sequential>

  </macrodef>

  <description>
    =========================================================================
      Macro:  zipTDIPwdPluginsPackage

      Purpose:  Make the zip of the TDI Plugins package in a specified location.
    =========================================================================
  </description>
  <macrodef name="zipTDIPwdPluginsPackage">
   <!-- The zip file name and location for the plugins base package. -->
   <attribute name="output.name.plugins.base"/>
   <!-- The zip file name and location for the plugins base package for windows specific files.-->
   <attribute name="output.name.plugins.base.win"/> 
   <!-- The zip file name and location for the plugins base package for unix specific files. -->
   <attribute name="output.name.plugins.base.unix"/>
   <!-- The zip file name and location for the windows plugins package. -->
   <attribute name="output.name.win.x86"/>
   <!-- The zip file name and location for the windows x86_64 plugins package. -->
   <attribute name="output.name.win.x86.64"/>
   <!-- The zip file name and location for the linux plugins package. -->
   <attribute name="output.name.linux.x86"/>
   <!-- The zip file name and location for the linux x86_64 plugins package. -->
   <attribute name="output.name.linux.x86.64"/>
   <!-- The zip file name and location for the aix plugins package. -->
   <attribute name="output.name.aix"/>
   <!-- The zip file name and location for the solaris sparc plugins package.-->
   <attribute name="output.name.solaris.sparc"/> 
   <!-- The zip file name and location for the hp parisc plugins package. -->
   <attribute name="output.name.hp.parisc"/>

   <sequential>
        <!-- Make sure the needed directory is created. 
        <delete dir="${project.root.pwd.plugin.export}"/>
        <mkdir dir="${project.root.pwd.plugin.export}"/>
		-->
		
        <!-- Copy over the files from the install/bin directory for windows
        <copy todir="${project.root.pwd.plugin.export}">
          <fileset dir="${project.root.pwd.plugin.src}">
            <include name="**/*.bat"/>
            <include name="**/*.sh"/>
            <include name="**/*.props"/>
            <include name="**/*.ldif"/>
            <include name="**/*.nsf"/>
          </fileset>
        </copy> -->

        <!-- delete any existing zip files. 
        <delete file="@{output.name.plugins.base}"/>
        <delete file="@{output.name.plugins.base.win}"/>
        <delete file="@{output.name.plugins.base.unix}"/>
        <delete file="@{output.name.win.x86}"/>
        <delete file="@{output.name.win.x86.64}"/>
        <delete file="@{output.name.linux.x86}"/>
        <delete file="@{output.name.linux.x86.64}"/>
        <delete file="@{output.name.aix}"/>
        <delete file="@{output.name.solaris.sparc}"/>
        <delete file="@{output.name.hp.parisc}"/>-->

        <!-- Create the base package.-->
        <zipCommonPluginJars plugin.zip="@{output.name.plugins.base}" plugin.prefix.dir="pwd_plugins" />
		
		
        <!-- Put the files in Windows format
        <fixcrlf srcdir="${project.root.pwd.plugin.export}/domino/etc"
                 eol="dos"
                 includes="*.props" />
        <fixcrlf srcdir="${project.root.pwd.plugin.export}/pam/etc"
                 eol="dos"
                 includes="*.props" />
        <fixcrlf srcdir="${project.root.pwd.plugin.export}/sun/etc"
                 eol="dos"
                 includes="*.props" />
        <fixcrlf srcdir="${project.root.pwd.plugin.export}/tds/etc"
                 eol="dos"
                 includes="*.props" /> -->

        <!-- Create the base package for windows. -->
        <zipCommonPluginFiles plugin.zip="@{output.name.plugins.base.win}" plugin.prefix.dir="pwd_plugins" plugin.file.format="dos" plugin.file.suffix="bat" />
		
        <!-- Put the files in Unix format
        <fixcrlf srcdir="${project.root.pwd.plugin.export}/domino/etc"
                 eol="unix"
                 includes="*.props" />
        <fixcrlf srcdir="${project.root.pwd.plugin.export}/pam/etc"
                 eol="unix"
                 includes="*.props" />
        <fixcrlf srcdir="${project.root.pwd.plugin.export}/sun/etc"
                 eol="unix"
                 includes="*.props" />
        <fixcrlf srcdir="${project.root.pwd.plugin.export}/tds/etc"
                 eol="unix"
                 includes="*.props" /> -->

        <!-- Create the base package for unix. -->
        <zipCommonPluginFiles plugin.zip="@{output.name.plugins.base.unix}" plugin.prefix.dir="pwd_plugins" plugin.file.format="unix" plugin.file.suffix="sh" />
		
		
        <!-- Create the windows x86 zip. 
        <zip zipfile="@{output.name.win.x86}" update="true">
            <zipfileset dir="${project.root.pwd.plugin.src}/tds/lib/win_x86"
                        prefix="pwd_plugins/IDS">
               <include name="idspwsync.dll"/>
            </zipfileset>
        </zip>-->

        <!-- Create the windows x86_64 zip. -->
        <zip zipfile="@{output.name.win.x86.64}" update="true">
            <!-- <zipfileset dir="${project.root.pwd.plugin.src}/tds/lib/win_x86_64"
                        prefix="pwd_plugins/IDS">
               <include name="idspwsync_64.dll"/> 
            </zipfileset> -->
            <zipfileset dir="${project.root.pwd.plugin.src}/windows/lib/win_x86_64"
                prefix="pwd_plugins/windows">
                <include name="tdipwflt_64.dll"/>
            </zipfileset>
        </zip>

        <!-- Create the linux x86 zip.
        <zip zipfile="@{output.name.linux.x86}"  update="true">
            <zipfileset dir="${project.root.pwd.plugin.src}/tds/lib/linux_x86"
                        prefix="pwd_plugins/IDS">
               <include name="libidspwsync.so"/>
            </zipfileset>
        </zip> -->

        <!-- Create the linux x86_64 zip.
        <zip zipfile="@{output.name.linux.x86.64}"  update="true">
            <zipfileset dir="${project.root.pwd.plugin.src}/tds/lib/linux_x86_64"
                        prefix="pwd_plugins/IDS">
               <include name="libidspwsync_64.so"/>
            </zipfileset>
        </zip> -->

        <!-- Create the linux aix zip. 
        <zip zipfile="@{output.name.aix}"  update="true">
            <zipfileset dir="${project.root.pwd.plugin.src}/tds/lib/aix"
                        prefix="pwd_plugins/IDS">
               <include name="libidspwsync.a.so"/>
               <include name="libidspwsync_64.a.so"/>
            </zipfileset>
        </zip>-->

        <!-- Create the solaris sparc zip. 
        <zip zipfile="@{output.name.solaris.sparc}"  update="true">
            <zipfileset dir="${project.root.pwd.plugin.src}/tds/lib/solaris_sparc"
                        prefix="pwd_plugins/IDS">
               <include name="libidspwsync.so"/>
               <include name="libidspwsync_64.so"/>
            </zipfileset>
        </zip>-->

        <!-- Create the linux hp parisc zip.
        <zip zipfile="@{output.name.hp.parisc}"  update="true">
            <zipfileset dir="${project.root.pwd.plugin.src}/tds/lib/hp_parisc"
                        prefix="pwd_plugins/IDS">
               <include name="libidspwsync.sl"/>
               <include name="libidspwsync_64.sl"/>
            </zipfileset>
        </zip> -->

   </sequential>
  </macrodef>

  <description>
    =========================================================================
      Macro:  tarTDIZOSPackage

      Purpose:  Make the tar of the files to be shipped on ZOS.
    =========================================================================
  </description>
  <macrodef name="tarTDIZOSPackage">
   <!-- The tar file name and location for the zos tar file. -->
   <attribute name="output.name"/>

   <sequential>
     <!-- delete any existing tar files. -->
     <delete file="@{output.name}"/>

     <!-- Start tarring up the files.. -->
     <tar destfile="@{output.name}">

         <tarfileset dir="${project.root.export.zos.bin}"
                     preserveLeadingSlashes="true">
             <include name="ibmdisrv"/>
             <include name="ibmdisrv_ascii"/>
         </tarfileset>

         <!-- tar up the files that go in the lib directory -->
         <tarfileset dir="${project.root}/non_java/zOSTSOCommandLine/bin"
                     prefix="libs"
                     preserveLeadingSlashes="true">
             <include name="libtdiappc.so"/>
         </tarfileset>

     </tar>
   </sequential>
  </macrodef>

  <!--ALAN's stuff-->
  <description>
    =========================================================================
      Macro:  zipTDIJREPackage

      Purpose:  Put the JRE zips in the proper location.
    =========================================================================
  </description>
  <macrodef name="zipTDIJREPackage">
      <sequential>
          <!-- These zips must already be present we're just copying them over. -->
          <copy todir="${project.root.export.support}/${SUPPORT_PACKAGE_NAME}">
              <fileset dir="${project.adks_home}/ibm/jres/support/150sr9">                  
                  <!-- List the JREs to be included in this fix -->

                  <!-- 1 or more of the following.....
                  <include name="Windows/JRE.zip"/>
                  <include name="Windows AMD64/JRE.zip"/>
                  <include name="zLinux/JRE.zip"/>
                  <include name="AIX/JRE.zip"/>
                  <include name="Solaris/JRE.zip"/>
                  <include name="Solaris Opteron/JRE.zip"/>
                  <include name="Linux PPC/JRE.zip"/>
                  <include name="Linux AMD64/JRE.zip"/>
                  <include name="Linux/JRE.zip"/>
                  <include name="HP-UX IA/JRE.zip"/>
                  <include name="HP-UX/JRE.zip"/>
                  -->
              </fileset>
          </copy>
      </sequential>
  </macrodef>

  <description>
    =========================================================================
      Macro:  zipTDIEWPPackage

      Purpose:  Put the EWP zips in the proper location.
    =========================================================================
  </description>
  <macrodef name="zipTDIEWPPackage">
      <sequential>
          <!-- This jar file must already be present we're just copying it over. -->
          <copy todir="${project.root.export.support}/${SUPPORT_PACKAGE_NAME}">
              <fileset dir="${project.adks_home}/ibm/lwi/support">                  
                  <!-- List the LWI jar file to be included in this fix -->
                  <!--
                  <include name="lwiUpdate7.1.1.4.jar"/>
                  -->
              </fileset>
          </copy>
      </sequential>
  </macrodef>

  <description>
    =========================================================================
      Macro:  zipTDICEUpdatePackage

      Purpose:  Make the zip of the TDI CE Update package in a specified location.
    =========================================================================
  </description>
  <macrodef name="zipTDICEUpdatePackage">
      <sequential>
          <!-- Nothing to do here...the zip will already exist, just copy it over -->  
          <copy todir="${project.root.export.support}/${SUPPORT_PACKAGE_NAME}">
              <fileset dir="${project.root.export}/ce_eclipse">
                  <include name="TDI_CEUpdateSite.zip" />
              </fileset>
          </copy>
      </sequential>
  </macrodef>

</project>
