# This file is used by autogen to determine where a JAR is created.
# Once defined, this file should remain unchanged.

# Entries are made one per line.  Comments are made by putting
# a 'hash' (#) in the first column of any line.  That line will be ignored.  
# Any whitespace is a non-comment line is ignored.  

base    =examples
path    =MSOutlook
